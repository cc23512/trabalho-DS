<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Configurações do cabeçalho -->
  </head>
  <body>
    <div class="container mt-5">
      <h2>Alterar Consulta</h2>

      <!-- Formulário para alterar consulta -->
      <form action="/alterarConsulta/<%= idConsulta %>" method="post">
        <!-- Campos do formulário (data, tipoDeConsulta, statusDaConsulta, etc.) -->
        <!-- Use valores padrão dos campos com os detalhes da consulta atual -->
        <label for="dataConsulta">Data da Consulta:</label>
        <input
          type="date"
          id="dataConsulta"
          name="dataConsulta"
          value="<%= consulta.dataConsulta ? moment(consulta.dataConsulta).format('YYYY-MM-DD') : '' %>"
          required
        />
        <br />

        <label for="horaConsulta">Horario da Consulta:</label>
        <input
          type="time"
          id="horaConsulta"
          name="horaConsulta"
          value="<%= consulta.horaConsulta %>"
          required
        />
        <br />

        <label for="tipoDeConsulta">Tipo de Consulta:</label>
        <input
          type="text"
          id="tipoDeConsulta"
          name="tipoDeConsulta"
          value="<%= consulta.tipoDeConsulta %>"
          required
        />
        <br />

        <label for="statusDaConsulta">Status da Consulta:</label>
        <select id="statusDaConsulta" name="statusDaConsulta" required>
          <option value="Ativa" <%= consulta.statusDaConsulta === 'Ativa' ? 'selected' : '' %>>Ativa</option>
          <option value="Realizada" <%= consulta.statusDaConsulta === 'Realizada' ? 'selected' : '' %>>Realizada</option>
          <option value="Cancelada" <%= consulta.statusDaConsulta === 'Cancelada' ? 'selected' : '' %>>Cancelada</option>
        </select>
        <!-- Outros campos do formulário -->
        <br />
        <button type="submit" class="btn btn-primary">Salvar Alterações</button>
      </form>

      <a href="/consultas" class="btn btn-secondary">Cancelar</a>
    </div>
  </body>
</html>
